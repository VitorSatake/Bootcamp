package edu.aluno.segundasemana;

public class Operadores {
    public static void main(String[] args) {
    /* 
    atribuição : =
    aritmético : + (adição), - (subtração), * (multiplicação), / (divisão)
    O operador + em tipo textos, realizará a concatenação dos textos

    Operador ternario : mesmo que um if basicamente, porem escrito em uma linha :
    Sempre a primeira expressaao é booleano, a == b
    Da mesma forma invés de retornar uma string pode retornar um int, int resultado = a == b ? 1 : 0;
    
    int a, b;
    a = 6;
    b = 6;
    String resultado = a == b ? "verdadeiro" : "falso";
    System.out.println(resultado);
    */

    
    }

}
